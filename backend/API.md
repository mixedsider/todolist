# 📚 Todo List API Documentation

## ⚠️ 공통 사항 & 인증 (Authentication)

이 애플리케이션은 **세션(Session) 기반 인증**을 사용합니다.

-   **인증 방식:** JSESSIONID 쿠키
-   **인증 필요 요청:** 로그인, 회원가입을 제외한 모든 API 요청 시
    유효한 JSESSIONID 쿠키가 포함되어야 합니다.
-   **권한 에러:** 세션이 없거나 유효하지 않을 경우 `401 Unauthorized`
    상태 코드를 반환합니다.

------------------------------------------------------------------------

## 👤 회원 (Members)

### 1. 회원가입

새로운 사용자를 등록합니다.

-   **URL:** `/api/v1/members/signup`
-   **Method:** POST
-   **Auth Required:** No

#### Request Body
```
  Field      Type     Description     Validation
  ---------- -------- --------------- ------------------------------
  email      String   사용자 이메일   이메일 형식 필수
  password   String   비밀번호        8자 이상, 특수문자 포함 필수
```

``` json
{
  "email": "user@example.com",
  "password": "password@123"
}
```

#### Response

-   `200 OK`: "회원가입 성공"
-   `400 Bad Request`: 유효성 검증 실패
-   `500 Internal Server Error`: 이메일 중복

------------------------------------------------------------------------

### 2. 로그인

이메일과 비밀번호로 로그인하고 세션을 생성합니다.

-   **URL:** `/api/v1/members/login`
-   **Method:** POST
-   **Auth Required:** No

#### Request Body
```
  Field      Type     Description
  ---------- -------- ---------------
  email      String   사용자 이메일
  password   String   비밀번호
```
``` json
{
  "email": "user@example.com",
  "password": "password@123"
}
```

#### Response

-   `200 OK`: "로그인 성공" (Set-Cookie: JSESSIONID 포함)
-   `401 Unauthorized`: 아이디 또는 비밀번호 불일치

------------------------------------------------------------------------

### 3. 로그아웃

현재 세션을 만료시킵니다.

-   **URL:** `/api/v1/members/logout`
-   **Method:** POST
-   **Auth Required:** Yes

#### Response

-   `200 OK`: "로그아웃 성공"

------------------------------------------------------------------------

## 📝 할 일 (Todos)

### 1. 내 할 일 목록 조회

로그인한 사용자의 모든 할 일을 조회합니다.

-   **URL:** `/api/v1/todos`
-   **Method:** GET
-   **Auth Required:** Yes

#### Response (JSON Array)

``` json
[
  {
    "id": 1,
    "content": "스프링 공부하기",
    "isCompleted": false,
    "createdAt": "2024-01-01T10:00:00"
    "completedAt": "2024-01-01T10:10:00"
  },
  {
    "id": 2,
    "content": "운동하기",
    "isCompleted": true,
    "createdAt": "2024-01-01T12:00:00"
    "completedAt": "2024-01-01T12:10:00"
  }
]
```

------------------------------------------------------------------------

### 2. 할 일 생성

새로운 Todo를 생성합니다.

-   **URL:** `/api/v1/todos`
-   **Method:** POST
-   **Auth Required:** Yes

#### Request Body
```
  Field     Type     Description   Validation
  --------- -------- ------------- ------------------
  content   String   할 일 내용    필수, 2글자 이상
```

``` json
{
  "content": "장보기"
}
```

#### Response

-   `200 OK`: 생성된 Todo ID
-   `400 Bad Request`: 내용이 2글자 미만

------------------------------------------------------------------------

### 3. 할 일 상태 변경 (Toggle)

Todo 항목의 완료 상태를 토글합니다.

-   **URL:** `/api/v1/todos/{todoId}`
-   **Method:** PATCH
-   **Auth Required:** Yes

#### Response

-   `200 OK`: true / false
-   `401 Unauthorized`: 로그인하지 않음 또는 본인 Todo가 아님

------------------------------------------------------------------------

### 4. 할 일 삭제

Todo 항목의 완료 상태를 토글합니다.

-   **URL:** `/api/v1/todos/{todoId}`
-   **Method:** DELETE
-   **Auth Required:** Yes

#### Response

-   `204 noContent`: 리턴값이 없음
-   `401 Unauthorized`: 로그인하지 않음 또는 본인 Todo가 아님

------------------------------------------------------------------------

## 🚨 Error Codes
```
  Status Code   Description
  ------------- ------------------------------------
  200           요청 성공
  400           잘못된 요청 (유효성 검증 실패 등)
  401           인증 실패 (로그인 필요, 권한 없음)
  500           서버 내부 오류
```